# Release Notes 草稿

## grok-keysmith v0.6.1

默认提示词按新一版契约更新。同日对照里，完整交付从 14/22 到 19/22；没有出现同一题连着两次都被拒的情况。

内核、边界、样本分析和创作这类请求，现在更不容易一开口就被判成「越狱」然后整段改道。服务端这几天也在收紧，旧契约在同一天只能到 14/22，这一版把丢掉的那截拿回来了。

首页改成产品说明：先看计划、确认再装、随时能撤。装完请开一个新会话。从旧版升级建议先卸载再装。

本次不更新桌面版安装包。
